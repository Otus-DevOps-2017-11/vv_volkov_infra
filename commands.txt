$ gcloud compute instances create reddit-app --boot-disk-size=10GB --image-family ubuntu-1604-lts --image-project=ubuntu-os-cloud --machine-type=g1-small --zone europe-west1-b --tags puma-server --restart-on-failure

$ ssh -t -i ~/.ssh/appuser -A appuser@public_ip

reddit-apt$ sudo apt update
reddit-apt$ sudo apt install -y ruby-full ruby-bundler build-essential

reddit-apt$ sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv EA312927
reddit-apt$ sudo bash -c 'echo "deb http://repo.mongodb.org/apt/ubuntu xenial/mongodb-org/3.2 multiverse" > /etc/apt/sources.list.d/mongodb-org-3.2.list' 

reddit-apt$ sudo apt update
reddit-apt$ sudo apt install -y mongodb-org

reddit-apt$ sudo systemctl start mongod
reddit-apt$ sudo systemctl enable mongod

reddit-apt$ git clone https://github.com/Otus-DevOps-2017-11/reddit.git

reddit-apt$ cd reddit && bundle install

reddit-apt$ puma -d



